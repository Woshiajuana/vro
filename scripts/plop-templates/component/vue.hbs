<template>
  <div class="{{ dashCase name }}">
    <span>{{ dashCase name }}</span>
  </div>
</template>

<script setup lang="ts">
  import { {{ camelCase name }}Props } from './types'

  // 如果定义属性 这里传 {{ camelCase name }}Props， 在 types 里完善类型
  defineProps({{ camelCase name }}Props)
</script>
